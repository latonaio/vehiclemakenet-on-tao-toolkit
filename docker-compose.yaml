version: "3"
services:
  vehiclemakenet-tao-toolkit:
    container_name: vehiclemakenet-tao-toolkit
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./:/app/src
    working_dir: /app/src
    tty: true
